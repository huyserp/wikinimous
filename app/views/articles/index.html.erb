<h1>Articles:</h1>
<ul>
<% @articles.each do |article| %>
  <li><%= link_to article.title, article_path(article.id) %> <%= link_to " * ", article_path(article.id), method: :delete %></li>
<% end %>
</ul>
